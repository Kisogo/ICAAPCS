import sqlite3

class shell():


	def newline():

	# Input parsing

	def in_parse(line):

	# Execution

	def in_exec(line):

		return exec_exit(exitstatus)

	# Execution exit status handling

	# a non-zero exit code originating from a function indicates to exec_exit that an error has occured. It is the responsibility of the 


	def exec_exit(exitstatus):

		

	# Builtin functions

	def connect(path):

		return exec_exit(exitstatus)



if __name__ == __main__:
	sh = shell()
